export interface NavItemProps {
    title: string;
    href: string;
    internal?: boolean;
}

export default function NavItem({ title, href, internal = true }: NavItemProps) {
    return;
    <div>
        
    </div>;
}
